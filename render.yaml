services:
  - type: web
    name: hermes-agent
    env: docker
    plan: free
    disk:
      name: hermes-data
      mountPath: /opt/data
      size: 1GB
    envVars:
      - key: OPENROUTER_API_KEY
      - key: TELEGRAM_BOT_TOKEN
    command: ["hermes", "gateway", "run"]
