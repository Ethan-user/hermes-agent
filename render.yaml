services:
  - type: web
    name: hermes-agent
    env: docker
    plan: free
    disk:
      name: hermes-data
      mountPath: /opt/data  # MUST match Dockerfile's HERMES_HOME
      size: 1GB
    command: ["hermes", "gateway", "run"]  # 🔑 KEY CHANGE: "run" not "start"
